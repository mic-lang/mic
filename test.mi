lifetime <depth p, depth q, kind a>
static struct X<p, a> func(struct X<p,a> c, int p* b) using r {
    b;
    struct X<static, dyn>* y;
    struct Y {int x;int y;};
    return (struct X<p,dyn>){0};
}
struct X<static, dyn>;
struct Y;
struct X<static, dyn> dyn* x;
lifetime <depth p, kind a>
struct X;
lifetime <depth p, kind a>
struct X {
    void p a* x;
    int a;
};

struct X<static, dyn>* y;

extern int printf(const char *, ...);

lifetime <depth p>
extern void p dyn* malloc(depth p, int size);  



int main () using p {
    func<p,p,static>;;
    struct X<p, dyn> b = func<p,p, dyn>((struct X<p,dyn>){.x=0}, null);
    goto label;
    struct Y p* const a;
    //int p* a;
    struct Y{int p* a;} p auto* b = a;
    using q {
        int q dyn* y = malloc<q>(q, 4);
    }
    a;
    
    //&5;
    func<static,static,auto>;
    int c[4];
    //a = c[3];
    c;

label : 
    for (int i=0;i<5;i++) using q {
        (*printf)("%d");
        if (++i==4)
        break;
        if (i<5)continue;
    }
    switch (0) using q {
        case 0:
        case 1:
        default:
        int printf = 0;
        break;
    }
return 0;
}

lifetime <depth p, kind a>
void swap(void p a* lhs, void p a* rhs) {
    void p a* temp = lhs;
    lhs = rhs;
    rhs = temp;
    return;
}

char c = '\n';
unsigned i = 5;
//struct X x= {.x=0,};
char* var= "abc";
int* const x;

typedef struct X<static,static> Z;
Z;
char *const ptr [4] ;
char (*const ptrq)[4];
void (*signal (int, void(*)(int a)))(int);
void (*(*p)[10])(int a, int b);


lifetime <depth p, depth q, kind a, kind b>
void h(void p a* q b* lhs, void p a* q b* rhs) using p {
    if (1) {
        if (1) {
            *lhs = *rhs;
            *rhs = null;
        } else {
            *lhs = *rhs;
            *rhs = null;
        }
    } else {
        *lhs = *rhs;
        *rhs = null;
    }
    malloc<static>(static, 4);
    return;
}

lifetime <depth p, kind a>
void i(void p a* drop ptr1) using p {
    return ptr1;
}

void j() using p{
    void p* ptr1, p* ptr2;
    i<p, auto>(ptr1);
    //ptr2 = ptr1;
}